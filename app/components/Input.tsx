import React from 'react';

const Inputs = () => {
    // Q: what props should this take?

    return (
        <>
            <h3>My daily uploads will get</h3>
            <input id="views-per-day" onChange={ } />
            <br />
            <h3>Views per day over</h3>
            <input id="total-days" onChange={ } />
            <h3>day(s)</h3>

        </>
    );
};

export default Inputs;